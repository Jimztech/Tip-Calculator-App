const billValue = document.getElementById("bill");
const numPeopleValue = document.getElementById("number");
const tipPercentage = document.getElementsByClassName("tip-button");
const customInput = document.getElementById("custom");
const amountOutput = document.getElementsByClassName("amount-output");
const totalOutput = document.getElementsByClassName("total-output");

function calculateTipPerPerson(billValue, numPeopleValue, tipPercentage) {
    const errorMessage = document.querySelector(".error-message");
}
